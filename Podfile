# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'FlickrPhotoGallery' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  pod 'SDWebImage', '~> 4.0'

  # Pods for FlickrPhotoGallery

  target 'FlickrPhotoGalleryTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'FlickrPhotoGalleryUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
